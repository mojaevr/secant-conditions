\RequirePackage{etoolbox}

\documentclass{mipt-thesis-bs}
\usepackage{float}

\makeatletter
\@twosidefalse
\@mparswitchfalse
\let\cleardoublepage\clearpage
\renewcommand{\addemptypage}{}
\makeatother


\makeatletter
\@ifundefined{c@v@normal}{\newcount\c@v@normal}{}%
\@ifundefined{c@v@bold}{\newcount\c@v@bold}{}%
\@ifundefined{c@v@italic}{\newcount\c@v@italic}{}%
\@ifundefined{c@v@bolditalic}{\newcount\c@v@bolditalic}{}%
\makeatother
% -----------------------------------------------------------------------------
\usepackage{mipt-thesis-biblatex}
\usepackage{algorithm}
\usepackage[noend]{algpseudocode}
\usepackage{booktabs}


\makeatletter
\newenvironment{breakablealgorithm}
  {% \begin{breakablealgorithm}
   \begin{center}
     \refstepcounter{algorithm}%
     \hrule height.8pt depth0pt \kern2pt
     \renewcommand{\caption}[2][\relax]{%
       {\raggedright\textbf{\ALG@name~\thealgorithm} ##2\par}%
       \ifx\relax##1\relax
         \addcontentsline{loa}{algorithm}{\protect\numberline{\thealgorithm}##2}%
       \else
         \addcontentsline{loa}{algorithm}{\protect\numberline{\thealgorithm}##1}%
       \fi
       \kern2pt\hrule\kern2pt
     }
  }{%
     \kern2pt\hrule\relax
   \end{center}
  }
\makeatother

\usepackage{hyperref}
% -----------------------------------------------------------------------------
\usepackage{xcolor}
\usepackage{enumitem}


\usepackage[protrusion=true,expansion=false]{microtype}


\setlength{\emergencystretch}{5em}

% --- Поля по Положению о ВКР (Прил. 2 §2): левое 30 мм, правое 15 мм,
% --- верхнее 20 мм, нижнее 20 мм. Дополнительный includehead/foot
% --- учитывает места под колонтитул и подвал, чтобы основное поле
% --- начиналось ровно от заданной границы.
\geometry{twoside=false,
          left=30mm,right=15mm,top=20mm,bottom=20mm,
          includehead,includefoot,headheight=15pt,headsep=6mm,footskip=8mm}

% --- Абзацный отступ (Прил. 2 §2) ------------------------------------------
\setlength{\parindent}{1.25cm}

\hypersetup{colorlinks=true,linkcolor=blue!60!black,citecolor=green!50!black}

% --- Подписи рисунков и таблиц по Положению (Прил. 2 §4) -------------------
% «Рисунок N — Название» по центру под рисунком; для таблиц «Таблица N —
% Название» слева, без отступа, в одну строку. Класс mipt-thesis-bs
% переопределяет \figurename как «Рис.», поэтому переопределяем после
% него (а также форматируем разделитель — endash вместо двоеточия).
\usepackage{caption}
% --- Разделитель «номер — название» через тире (em-dash) по Положению
% --- о ВКР, Прил. 2: «...в одну строку с её номером через тире»;
% --- пример из Положения «Рисунок 1 — Детали прибора». Стандартный
% --- labelsep=endash даёт короткое en-dash, поэтому объявляем
% --- собственный разделитель с настоящим тире (\nobreakspace перед ним
% --- удерживает номер с тире на одной строке).
\DeclareCaptionLabelSeparator{gostdash}{~--- }
\captionsetup{labelsep=gostdash,font=normal}
\captionsetup[table]{justification=raggedright,singlelinecheck=false}
\addto\captionsrussian{%
  \renewcommand{\figurename}{Рисунок}%
  \renewcommand{\tablename}{Таблица}%
}

% --- Нумерация страниц по центру нижнего поля (Прил. 2 §3) -----------------
% Класс mipt-thesis-bs ставит \thepage в верхний внешний угол. Положение
% требует «середина нижнего поля». Переопределяем стиль `miptthesis` после
% его декларации в классе и принудительно подключаем заново через
% \pagestyle.
\fancypagestyle{miptthesis}{%
  \fancyhf{}%
  \fancyfoot[C]{\thepage}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
}
\fancypagestyle{plain}{%
    \fancyhf{}%                                          
    \fancyfoot[C]{\thepage}%
    \renewcommand{\headrulewidth}{0pt}%
    \renewcommand{\footrulewidth}{0pt}%
  }
\pagestyle{miptthesis}

%--- Теоремные окружения ---------------------------------------------------
% ГОСТ-совместимый вариант: чёрный жирный serif-заголовок в основном
% семействе шрифтов (Latin Modern), без цвета и без sans-serif в теле
% научной работы. Дифференциация трёх классов утверждений — через
% штатные стили amsthm:
%   * miptmain (≡ plain)      — теоремы/леммы/следствия/утверждения/
%                                гипотезы: жирный заголовок на отдельной
%                                строке + курсивное тело;
%   * miptdef  (≡ definition) — определения и предположения: жирный
%                                заголовок на отдельной строке + прямое тело;
%   * miptrem  (≡ remark)     — замечания: курсивный заголовок обычного
%                                веса inline + прямое тело (намеренно
%                                «легче», без линий-разделителей).
% Заголовок на отдельной строке достигается передачей \newline в качестве
% «head/body sep» (7-й аргумент). Тонкие горизонтальные линии-разделители
% сверху и снизу добавляются ниже через \BeforeBeginEnvironment /
% \AfterEndEnvironment из etoolbox — для главных утверждений, определений
% и доказательств, но НЕ для замечаний.
% Сигнатура \newtheoremstyle:
%   {above}{below}{body font}{indent}{header font}{punct}{head/body sep}{custom}
\newtheoremstyle{miptmain}%
  {0.6\baselineskip}{0.4\baselineskip}{\itshape}{0pt}%
  {\large\bfseries}{.}{\newline}{}
\newtheoremstyle{miptdef}%
  {0.6\baselineskip}{0.4\baselineskip}{\normalfont}{0pt}%
  {\large\bfseries}{.}{\newline}{}
\newtheoremstyle{miptrem}%
  {0.6\baselineskip}{0.4\baselineskip}{\normalfont}{0pt}%
  {\itshape}{.}{0.5em}{}

\theoremstyle{miptmain}
\newtheorem{theorem}{Теорема}[chapter]
\newtheorem{lemma}[theorem]{Лемма}
\newtheorem{corollary}[theorem]{Следствие}
\newtheorem{conjecture}[theorem]{Гипотеза}
\newtheorem{proposition}{Утверждение}[chapter]

\theoremstyle{miptdef}
\newtheorem{definition}[theorem]{Определение}
\newtheorem{assumption}{Предположение}[chapter]

\theoremstyle{miptrem}
\newtheorem{remark}[theorem]{Замечание}

% --- Без левого отступа: блоки теорем/лемм/доказательств идут по той же
% --- левой границе, что и основной текст. Дифференциация остаётся только
% --- по \newtheoremstyle (жирный заголовок \large на отдельной строке +
% --- курсив/прямой шрифт тела).

% --- Доказательства: жирный заголовок «Доказательство.» на отдельной
% --- строке и заметный QED ------------------------------------------------
\renewcommand{\proofname}{Доказательство}
\renewcommand{\qedsymbol}{\ensuremath{\blacksquare}}
\makeatletter
\renewenvironment{proof}[1][\proofname]{\par
  \pushQED{\qed}%
  \normalfont \topsep6\p@\@plus6\p@\relax
  \trivlist
  \item[\hskip\labelsep
        \large\bfseries
    #1\@addpunct{.}]\mbox{}\par\nobreak\noindent\ignorespaces
}{%
  \popQED\endtrivlist\@endpefalse
}
\makeatother

%--- Математические макросы ---
\newcommand{\RR}{\mathbb{R}}
\newcommand{\Fro}[1]{\left\|#1\right\|_F}
\newcommand{\Norm}[1]{\left\|#1\right\|}
\newcommand{\pinv}[1]{#1^{\dagger}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\col}{\operatorname{col}}
\newcommand{\rank}{\operatorname{rank}}
\newcommand{\diag}{\operatorname{diag}}
\newcommand{\Jstar}{J^{*}}
\newcommand{\Delk}{\Delta_{k}}
\newcommand{\Delkp}{\Delta_{k+1}}
\newcommand{\rhok}{\rho^{\ast}_k}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator*{\argmin}{arg\,min}

%--- Титульные данные ---
\title{Исследование глобальных квазиньютоновских методов оптимизации для задач оптимизации и вариационных неравенств}
\author{Можаев~Р.\,М.}
\supervisor{Камзолов~Д.\,И.}
\groupnum{М05-404а}
\faculty{Физтех-школа прикладной математики и информатики}
\department{Кафедра проблем передачи информации и анализа данных}


\makeatletter
\mt@flush@groups
\makeatother
\begin{document}

%% ============================================================
%%  Структурные элементы по Положению о ВКР МФТИ (Прил. 1 §3):
%%  титульный → аннотация → содержание → обозначения и сокращения
%%  → основной текст. Раскручиваем \titlecontents (= titlepage +
%%  tableofcontents бандлом) на отдельные шаги, чтобы вставить
%%  аннотацию ПЕРЕД содержанием.
%%  По Прил. 2 §3: «на следующей [после титульного] странице ставится
%%  цифра «2»» — поэтому \pagestyle{miptthesis} включаем сразу после
%%  титульного листа, до аннотации.
%% ============================================================
\titlepage
\newpage
\pagestyle{miptthesis}

\include{sections/abstract_ru}


\tableofcontents

\include{sections/notation}

%% ============================================================
%%  Введение
%% ============================================================
\include{sections/introduction}


%% ============================================================
%%  Главы
%% ============================================================
\include{sections/ch1_jacobian}
\include{sections/ch2_sp_broyden}
\include{sections/ch3_symmetric}
\include{sections/ch4_viqa}

%% ============================================================
%%  Заключение
%% ============================================================
\include{sections/conclusion}



%% ============================================================
%%  Библиография
%% ============================================================
\input{sections/bibliography}

\end{document}
